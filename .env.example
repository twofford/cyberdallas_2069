# Example:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cyberdallas?schema=public"

# Used to sign/verify bearer auth tokens for the GraphQL API.
# Generate a strong value for local dev with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
# Never reuse this value in production.
# AUTH_SECRET="your-secret-here"

# Base URL for generating absolute links in emails.
# Set this to whatever port your dev server is running on.
# Example (if you run `npm run dev -- -p 3001`):
# APP_BASE_URL="http://localhost:3001"

# SMTP settings for sending invite emails (Nodemailer).
# For local dev with MailHog/Mailpit, this is typically:
# SMTP_HOST="localhost"
# SMTP_PORT="1025"
# SMTP_SECURE="false"
# SMTP_FROM="CyberDallas 2069 <noreply@cyberdallas.local>"
# DISABLE_EMAIL="true"
#
# If your SMTP server requires auth:
# SMTP_USER=""
# SMTP_PASS=""

# Gmail SMTP (sufficient for dev/manual testing)
# Note: Use a Google App Password (recommended), not your normal account password.
# APP_BASE_URL="http://localhost:3001"
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER="taylor.wofford@gmail.com"
# SMTP_PASS="your-google-app-password"
# SMTP_FROM="taylor.wofford@gmail.com"

# Send a test invite email (no UI)
# 1) Start the dev server on the same port as APP_BASE_URL (example 3001):
#    npm run dev -- -p 3001
# 2) Run:
#    npm run email:test-invite
